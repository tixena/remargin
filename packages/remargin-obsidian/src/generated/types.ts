// Auto-generated by generate_types. Do not edit.

/**
 * Whether the comment author is a human or an AI agent.
 * 
 * JSON Schema:
 * {
 *   "enum": [
 *     "Agent",
 *     "Human"
 *   ],
 *   "type": "string"
 * }
 **/
export type AuthorType =
  /*
  * An AI agent participant.
  */
  | "Agent"
  /*
  * A human participant.
  */
  | "Human";
/**
 * An acknowledgment of a comment by another participant.
 * 
 * JSON Schema:
 * {
 *   "additionalProperties": false,
 *   "properties": {
 *     "author": {
 *       "type": "string"
 *     },
 *     "ts": {
 *       "format": "date-time",
 *       "type": "string"
 *     }
 *   },
 *   "required": [
 *     "author",
 *     "ts"
 *   ],
 *   "type": "object"
 * }
 **/


export type Acknowledgment = {
  /**
 * Author who acknowledged.
 * 
**/
  author: string;
  /**
 * Timestamp of the acknowledgment.
 * 
**/
  ts: string;

};
/**
 * A parsed Remargin comment with all metadata fields.
 * The serde [`Serialize`] implementation emits JSON that matches the
 * `Comment` schema generated by `tixschema` (see `generate_types` bin):
 * field names stay `snake_case`, `author_type` is `PascalCase`
 * (`Agent`/`Human`), `Option` fields are omitted when `None`, and
 * required collections (`ack`, `attachments`, `to`, `reactions`) are
 * always present.
 * 
 * JSON Schema:
 * {
 *   "additionalProperties": false,
 *   "properties": {
 *     "ack": {
 *       "items": {
 *         "additionalProperties": false,
 *         "properties": {
 *           "author": {
 *             "type": "string"
 *           },
 *           "ts": {
 *             "format": "date-time",
 *             "type": "string"
 *           }
 *         },
 *         "required": [
 *           "author",
 *           "ts"
 *         ],
 *         "type": "object"
 *       },
 *       "type": "array"
 *     },
 *     "attachments": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     },
 *     "author": {
 *       "type": "string"
 *     },
 *     "author_type": {
 *       "enum": [
 *         "Agent",
 *         "Human"
 *       ],
 *       "type": "string"
 *     },
 *     "checksum": {
 *       "type": "string"
 *     },
 *     "content": {
 *       "type": "string"
 *     },
 *     "fence_depth": {
 *       "type": "integer"
 *     },
 *     "id": {
 *       "type": "string"
 *     },
 *     "line": {
 *       "type": "integer"
 *     },
 *     "reactions": {
 *       "additionalProperties": {
 *         "items": {
 *           "type": "string"
 *         },
 *         "type": "array"
 *       },
 *       "type": "object"
 *     },
 *     "reply_to": {
 *       "type": "string"
 *     },
 *     "signature": {
 *       "type": "string"
 *     },
 *     "thread": {
 *       "type": "string"
 *     },
 *     "to": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     },
 *     "ts": {
 *       "format": "date-time",
 *       "type": "string"
 *     }
 *   },
 *   "required": [
 *     "ack",
 *     "attachments",
 *     "author",
 *     "author_type",
 *     "checksum",
 *     "content",
 *     "fence_depth",
 *     "id",
 *     "line",
 *     "reactions",
 *     "to",
 *     "ts"
 *   ],
 *   "type": "object"
 * }
 **/


export type Comment = {
  /**

**/
  ack: Array<Acknowledgment>;
  /**

**/
  attachments: Array<string>;
  /**
 * Author name or identifier.
 * 
**/
  author: string;
  /**
 * Whether the author is human or agent.
 * 
**/
  author_type: AuthorType;
  /**
 * Content integrity checksum (e.g. "sha256:a1b2c3d4...").
 * 
**/
  checksum: string;
  /**
 * Comment body text (everything after the YAML `---` separator).
 * 
**/
  content: string;
  /**
 * Number of backticks in the wrapping fence (3, 4, 5, ...).
 * 
**/
  fence_depth: number;
  /**
 * Unique short identifier (e.g. "abc").
 * 
**/
  id: string;
  /**
 * 1-indexed line number of the opening fence in the source document.
 * Zero means "not yet placed" (e.g. newly created, before write).
 * 
**/
  line: number;
  /**
 * Emoji reactions mapped to lists of author IDs.
 * 
**/
  reactions: Partial<Record<string, Array<string>>>;
  /**

**/
  reply_to: string | undefined;
  /**

**/
  signature: string | undefined;
  /**

**/
  thread: string | undefined;
  /**

**/
  to: Array<string>;
  /**
 * Timestamp when the comment was created.
 * 
**/
  ts: string;

};
/**
 * A single entry from a directory listing.
 * Serializes to JSON that matches the `ListEntry` tixschema: `path` is
 * rendered as a string and all `Option` fields are skipped when `None`
 * so the generated Zod `strictObject` schema accepts them as `undefined`.
 * 
 * JSON Schema:
 * {
 *   "additionalProperties": false,
 *   "properties": {
 *     "is_dir": {
 *       "type": "boolean"
 *     },
 *     "path": {
 *       "type": "string"
 *     },
 *     "remargin_last_activity": {
 *       "type": "string"
 *     },
 *     "remargin_pending": {
 *       "type": "integer"
 *     },
 *     "size": {
 *       "type": "integer"
 *     }
 *   },
 *   "required": [
 *     "is_dir",
 *     "path"
 *   ],
 *   "type": "object"
 * }
 **/


export type ListEntry = {
  /**
 * Whether this entry is a directory.
 * 
**/
  is_dir: boolean;
  /**
 * Relative path from the base directory.
 * 
**/
  path: string;
  /**

**/
  remargin_last_activity: string | undefined;
  /**

**/
  remargin_pending: number | undefined;
  /**

**/
  size: number | undefined;

};
/**
 * Owned comment data for inclusion in expanded query results.
 * Cloned from parsed [`parser::Comment`] because the parsed document is
 * dropped after processing each file.
 * The serde [`Serialize`] implementation emits JSON that matches the
 * `ExpandedComment` schema generated by `tixschema`: `snake_case` field
 * names, `PascalCase` `author_type` variants, `file` as a string, and
 * `Option` fields skipped when `None`.
 * 
 * JSON Schema:
 * {
 *   "additionalProperties": false,
 *   "properties": {
 *     "ack": {
 *       "items": {
 *         "additionalProperties": false,
 *         "properties": {
 *           "author": {
 *             "type": "string"
 *           },
 *           "ts": {
 *             "format": "date-time",
 *             "type": "string"
 *           }
 *         },
 *         "required": [
 *           "author",
 *           "ts"
 *         ],
 *         "type": "object"
 *       },
 *       "type": "array"
 *     },
 *     "attachments": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     },
 *     "author": {
 *       "type": "string"
 *     },
 *     "author_type": {
 *       "enum": [
 *         "Agent",
 *         "Human"
 *       ],
 *       "type": "string"
 *     },
 *     "checksum": {
 *       "type": "string"
 *     },
 *     "content": {
 *       "type": "string"
 *     },
 *     "file": {
 *       "type": "string"
 *     },
 *     "id": {
 *       "type": "string"
 *     },
 *     "line": {
 *       "type": "integer"
 *     },
 *     "reactions": {
 *       "additionalProperties": {
 *         "items": {
 *           "type": "string"
 *         },
 *         "type": "array"
 *       },
 *       "type": "object"
 *     },
 *     "reply_to": {
 *       "type": "string"
 *     },
 *     "signature": {
 *       "type": "string"
 *     },
 *     "thread": {
 *       "type": "string"
 *     },
 *     "to": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     },
 *     "ts": {
 *       "format": "date-time",
 *       "type": "string"
 *     }
 *   },
 *   "required": [
 *     "ack",
 *     "attachments",
 *     "author",
 *     "author_type",
 *     "checksum",
 *     "content",
 *     "file",
 *     "id",
 *     "line",
 *     "reactions",
 *     "to",
 *     "ts"
 *   ],
 *   "type": "object"
 * }
 **/


export type ExpandedComment = {
  /**

**/
  ack: Array<Acknowledgment>;
  /**

**/
  attachments: Array<string>;
  /**
 * Author name or identifier.
 * 
**/
  author: string;
  /**
 * Whether the author is human or agent.
 * 
**/
  author_type: AuthorType;
  /**
 * Content integrity checksum.
 * 
**/
  checksum: string;
  /**
 * Comment body text.
 * 
**/
  content: string;
  /**
 * Relative path to the file this comment belongs to.
 * 
**/
  file: string;
  /**
 * Unique short identifier.
 * 
**/
  id: string;
  /**
 * 1-indexed line number in the source document.
 * 
**/
  line: number;
  /**
 * Emoji reactions mapped to lists of author IDs.
 * 
**/
  reactions: Partial<Record<string, Array<string>>>;
  /**

**/
  reply_to: string | undefined;
  /**

**/
  signature: string | undefined;
  /**

**/
  thread: string | undefined;
  /**

**/
  to: Array<string>;
  /**
 * Timestamp when the comment was created.
 * 
**/
  ts: string;

};
/**
 * A single result from a cross-document query.
 * Serializes to JSON that matches the `QueryResult` tixschema.
 * 
 * JSON Schema:
 * {
 *   "additionalProperties": false,
 *   "properties": {
 *     "comment_count": {
 *       "type": "integer"
 *     },
 *     "comments": {
 *       "items": {
 *         "additionalProperties": false,
 *         "properties": {
 *           "ack": {
 *             "items": {
 *               "additionalProperties": false,
 *               "properties": {
 *                 "author": {
 *                   "type": "string"
 *                 },
 *                 "ts": {
 *                   "format": "date-time",
 *                   "type": "string"
 *                 }
 *               },
 *               "required": [
 *                 "author",
 *                 "ts"
 *               ],
 *               "type": "object"
 *             },
 *             "type": "array"
 *           },
 *           "attachments": {
 *             "items": {
 *               "type": "string"
 *             },
 *             "type": "array"
 *           },
 *           "author": {
 *             "type": "string"
 *           },
 *           "author_type": {
 *             "enum": [
 *               "Agent",
 *               "Human"
 *             ],
 *             "type": "string"
 *           },
 *           "checksum": {
 *             "type": "string"
 *           },
 *           "content": {
 *             "type": "string"
 *           },
 *           "file": {
 *             "type": "string"
 *           },
 *           "id": {
 *             "type": "string"
 *           },
 *           "line": {
 *             "type": "integer"
 *           },
 *           "reactions": {
 *             "additionalProperties": {
 *               "items": {
 *                 "type": "string"
 *               },
 *               "type": "array"
 *             },
 *             "type": "object"
 *           },
 *           "reply_to": {
 *             "type": "string"
 *           },
 *           "signature": {
 *             "type": "string"
 *           },
 *           "thread": {
 *             "type": "string"
 *           },
 *           "to": {
 *             "items": {
 *               "type": "string"
 *             },
 *             "type": "array"
 *           },
 *           "ts": {
 *             "format": "date-time",
 *             "type": "string"
 *           }
 *         },
 *         "required": [
 *           "ack",
 *           "attachments",
 *           "author",
 *           "author_type",
 *           "checksum",
 *           "content",
 *           "file",
 *           "id",
 *           "line",
 *           "reactions",
 *           "to",
 *           "ts"
 *         ],
 *         "type": "object"
 *       },
 *       "type": "array"
 *     },
 *     "last_activity": {
 *       "format": "date-time",
 *       "type": "string"
 *     },
 *     "path": {
 *       "type": "string"
 *     },
 *     "pending_count": {
 *       "type": "integer"
 *     },
 *     "pending_for": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     }
 *   },
 *   "required": [
 *     "comment_count",
 *     "comments",
 *     "path",
 *     "pending_count",
 *     "pending_for"
 *   ],
 *   "type": "object"
 * }
 **/


export type QueryResult = {
  /**
 * Total number of comments in the document.
 * 
**/
  comment_count: number;
  /**

**/
  comments: Array<ExpandedComment>;
  /**

**/
  last_activity: string | undefined;
  /**
 * Relative path to the document.
 * 
**/
  path: string;
  /**
 * Number of pending (unacked) comments.
 * 
**/
  pending_count: number;
  /**

**/
  pending_for: Array<string>;

};
/**
 * Where a match was found within the document.
 * 
 * JSON Schema:
 * {
 *   "enum": [
 *     "Body",
 *     "Comment"
 *   ],
 *   "type": "string"
 * }
 **/
export type MatchLocation =
  /*
  * In document body text (outside any comment block).
  */
  | "Body"
  /*
  * Inside a comment block.
  */
  | "Comment";
/**
 * A single search match.
 * Serializes to JSON that matches the `SearchMatch` tixschema:
 * `path` as a string, `location` as `"Body"` or `"Comment"`, and
 * `comment_id` skipped when `None`.
 * 
 * JSON Schema:
 * {
 *   "additionalProperties": false,
 *   "properties": {
 *     "after": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     },
 *     "before": {
 *       "items": {
 *         "type": "string"
 *       },
 *       "type": "array"
 *     },
 *     "comment_id": {
 *       "type": "string"
 *     },
 *     "line": {
 *       "type": "integer"
 *     },
 *     "location": {
 *       "enum": [
 *         "Body",
 *         "Comment"
 *       ],
 *       "type": "string"
 *     },
 *     "path": {
 *       "type": "string"
 *     },
 *     "text": {
 *       "type": "string"
 *     }
 *   },
 *   "required": [
 *     "after",
 *     "before",
 *     "line",
 *     "location",
 *     "path",
 *     "text"
 *   ],
 *   "type": "object"
 * }
 **/


export type SearchMatch = {
  /**

**/
  after: Array<string>;
  /**

**/
  before: Array<string>;
  /**

**/
  comment_id: string | undefined;
  /**
 * 1-indexed line number of the match within the file.
 * 
**/
  line: number;
  /**
 * Whether the match is in body text or a comment.
 * 
**/
  location: MatchLocation;
  /**
 * Relative file path.
 * 
**/
  path: string;
  /**
 * The full text of the matching line.
 * 
**/
  text: string;

};
